/**
 * Convenience re-export of the sector percentiles context hook.
 * Import from here or directly from the context — both work.
 */
export { useSectorPercentiles } from '@/lib/contexts/SectorPercentilesContext';
