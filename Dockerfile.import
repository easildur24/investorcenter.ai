FROM alpine:3.18

# Install ca-certificates for HTTPS
RUN apk add --no-cache ca-certificates

# Copy the import binary
COPY bin/import-tickers /usr/local/bin/import-tickers

# Make it executable
RUN chmod +x /usr/local/bin/import-tickers

ENTRYPOINT ["/usr/local/bin/import-tickers"]