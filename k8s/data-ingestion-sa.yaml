apiVersion: v1
kind: ServiceAccount
metadata:
  name: data-ingestion-sa
  namespace: investorcenter
  annotations:
    # IRSA: This role must be created in AWS IAM with:
    # - Trust policy for EKS OIDC provider
    # - Policy allowing s3:PutObject, s3:GetObject on investorcenter-raw-data/*
    eks.amazonaws.com/role-arn: arn:aws:iam::360358043271:role/data-ingestion-s3-role
