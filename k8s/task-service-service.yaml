apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: investorcenter
  labels:
    app: task-service
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
